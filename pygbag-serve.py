I notice that we need to modify our approach since we're using Pygbag to create a web-based game. 

Instead of using Python's built-in HTTP server, we should use Pygbag's serve command to properly handle the WebAssembly compilation and serving. 
Let me fix this.

